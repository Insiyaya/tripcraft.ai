VITE_API_URL=http://localhost:8000

# Optional — enables the Google sign-in button. Must match the backend's
# GOOGLE_CLIENT_ID exactly. Leave empty to show only the email/password form.
VITE_GOOGLE_CLIENT_ID=
